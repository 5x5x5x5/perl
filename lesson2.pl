#!usr/bin/perl
# Write an if-statement that executes only if the value in $a is numerically greater than $b

if ($a > $b) { };

if($a lt $b) {
      print "hello\n";
    } else {
      print "bye\n";
    }

 
chomp($input = <STDIN>);
$answer = "Hello there,";
if ($input eq $answer) {
   print "XXX";
   } else {
   print "YYY";
 }

